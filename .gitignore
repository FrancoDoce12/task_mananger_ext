# Ignore node_modules folder
node_modules

# Ignore all files recursively in build folder
build/**/*.js

# Don't ignore folders
!build/**/*/

# Don't ignore html files inside the build folder
!build/**/*.html

# Don't ignore the manifest file inside the build folder
!build/manifest.json